---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: node-exporter
  labels:
    prometheus/shared: "true"
spec:
  endpoints:
  - interval: 30s
    port: prometheus
  selector:
    matchLabels:
      app: node-exporter
